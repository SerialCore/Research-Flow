<Page
    x:Class="Research_Flow.Configure"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Research_Flow"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Background="Transparent" VerticalAlignment="Center">
            <PersonPicture x:Name="accountIcon" Tapped="Login_Tapped" Height="150" Width="150"/>
            <TextBlock x:Name="accountName" FontSize="20" Margin="0,20,0,0" HorizontalAlignment="Center"/>
            <TextBlock x:Name="accountStatu" Text="Tap the icon to login OneDrive" FontSize="20" Margin="0,10,0,20" HorizontalAlignment="Center"/>
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <ProgressBar x:Name="syncStatu" Width="200" Visibility="Collapsed"/>
                <TextBlock x:Name="syncCount" Margin="10,0,0,0"/>
            </StackPanel>
        </StackPanel>

        <TextBlock x:Name="configState" FontSize="20" Margin="20" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>

        <HyperlinkButton Content="Skip this" FontSize="20" Click="SkipAccount" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="50"/>
    </Grid>
    
</Page>
