<Page
    x:Class="Research_Flow.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Research_Flow"
    xmlns:data="using:LogicService.Data"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="FullPage" Background="{StaticResource ApplicationPageBackgroundThemeBrush}">

        <NavigationView x:Name="NavView"
                        Background="{ThemeResource SystemAccentColorLight1}"
                        PaneDisplayMode="LeftCompact"
                        Loaded="NavView_Loaded"
                        SelectionChanged="NavView_SelectionChanged"
                        IsBackEnabled="{x:Bind ContentFrame.CanGoBack,Mode=OneWay}"
                        BackRequested="NavView_BackRequested">

            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="Overview" Content="Overview" Tag="Overview">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEB68;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="TagCenter" Content="Tag Center" Tag="Tag">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8EC;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="TopicCase" Content="Topic Case" Tag="Topic">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEB50;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItemSeparator/>
                <NavigationViewItem x:Uid="PaperBox" Content="Paper Box" Tag="PaperBox">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7BE;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="RssService" Content="RSS Service" Tag="RSS">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE701;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="SearchEngine" Content="Search Engine" Tag="Search">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEDE4;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="CrawlerService" Content="Crawler Service" Tag="Crawler">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE24A;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItemSeparator/>
                <NavigationViewItem x:Uid="DrawableNote" Content="Drawable Note" Tag="Note">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEE56;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="PictureAlbum" Content="Picture Album" Tag="Picture">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEB9F;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.PaneHeader>
                <TextBlock x:Uid="AppDisplayName" Text="Research Flow (preview)" VerticalAlignment="Center" Margin="8,0,0,0"/>
            </NavigationView.PaneHeader>

            <NavigationView.PaneFooter>
                <StackPanel>
                    <ListViewItem Tapped="Flyout_Tapped">
                        <StackPanel Orientation="Horizontal">
                            <PersonPicture x:Name="accountPhoto1" Height="20" Width="20"/>
                            <TextBlock x:Name="accountName1" FontWeight="Bold" Margin="16,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <FlyoutBase.AttachedFlyout>
                            <Flyout>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <PersonPicture Grid.Row="0" Grid.RowSpan="2" x:Name="accountPhoto2" Height="40" Width="40" Margin="5"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" x:Name="accountName2" FontWeight="Bold" FontSize="14" VerticalAlignment="Bottom" Margin="5,0,0,0"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="accountEmail" VerticalAlignment="Top" Margin="5,0,0,0"/>
                                    <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal">
                                        <HyperlinkButton x:Uid="LoginButton" Content="Login" Click="AccountLogin_Click" Margin="5,0,10,0"/>
                                        <HyperlinkButton x:Uid="LogoutButton" Content="Logout" Click="AccountLogout_Click"/>
                                    </StackPanel>
                                </Grid>
                            </Flyout>
                        </FlyoutBase.AttachedFlyout>
                    </ListViewItem>
                    <StackPanel Orientation="Horizontal">
                        <AppBarButton x:Uid="ScreenShotButton" Icon="AttachCamera" Click="ScreenShot_Upload" ToolTipService.ToolTip="ScreenShot" Width="42" Height="42"/>
                        <!--<AppBarButton Icon="SwitchApps" Click="Open_FlowBlade" ToolTipService.ToolTip="Flow" Width="42" Height="42"/>-->
                        <AppBarButton x:Uid="ChatButton" Icon="Message" Click="Open_ChatBlade" ToolTipService.ToolTip="Chat" Width="42" Height="42"/>
                        <AppBarButton x:Uid="FullScreen" Icon="FullScreen" Click="FullScreen_Click" ToolTipService.ToolTip="FullScreen" Height="42" Width="42"/>
                    </StackPanel>
                </StackPanel>
            </NavigationView.PaneFooter>

            <NavigationView.AutoSuggestBox>
                <AutoSuggestBox x:Uid="CrawlSearch" x:Name="crawlsearch" QueryIcon="Find" PlaceholderText="search in your crawler database" QuerySubmitted="CrawlSearch_QuerySubmitted"/>
            </NavigationView.AutoSuggestBox>

            <Frame x:Name="ContentFrame" Navigated="NavView_Navigated">
                <Frame.ContentTransitions>
                    <TransitionCollection>
                        <NavigationThemeTransition/>
                    </TransitionCollection>
                </Frame.ContentTransitions>
            </Frame>

        </NavigationView>

        <toolkit:BladeView x:Name="BladeView" BladeMode="Normal" AutoCollapseCountThreshold="2" HorizontalAlignment="Right" Margin="0,32,0,0">
            <toolkit:BladeItem x:Uid="FlowBlade" x:Name="FlowBlade" Header="Flow" TitleBarVisibility="Visible" IsOpen="False">
                <Grid Margin="10">
                </Grid>
            </toolkit:BladeItem>
            <toolkit:BladeItem x:Name="ChatBlade" Header="Chat" TitleBarVisibility="Visible" IsOpen="False">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="32"/>
                    </Grid.RowDefinitions>
                    <Image Source="ms-appx:///Images/ResearchFlow_logo.jpg"/>
                    <ScrollViewer x:Name="chatscroll" Grid.Row="0">
                        <ListView x:Name="chatview" SelectionMode="None" SizeChanged="ChatView_SizeChanged">
                            <ListView.Resources>
                                <DataTemplate x:Key="ChatDataTemplate">
                                    <StackPanel HorizontalAlignment="Left" Margin="0,5,0,10">
                                        <Grid Grid.Row="0" CornerRadius="4" Background="{ThemeResource SystemAccentColorDark1}" Padding="10">
                                            <TextBlock Text="{Binding Comment}" Foreground="White" IsTextSelectionEnabled="True" TextWrapping="Wrap" MaxWidth="250"/>
                                        </Grid>
                                        <TextBlock Grid.Row="1" Text="{Binding Published}" Foreground="{ThemeResource SystemAccentColorDark1}"/>
                                    </StackPanel>
                                </DataTemplate>

                                <DataTemplate x:Key="SelfChatDataTemplate">
                                    <StackPanel HorizontalAlignment="Right" Margin="0,5,0,10">
                                        <Grid Grid.Row="0" CornerRadius="4" Background="{ThemeResource SystemAccentColorLight1}" Padding="10">
                                            <TextBlock Text="{Binding Comment}" Foreground="White" IsTextSelectionEnabled="True" TextWrapping="Wrap" MaxWidth="250" HorizontalAlignment="Right"/>
                                        </Grid>
                                        <TextBlock Grid.Row="1" Text="{Binding Published}" Foreground="{ThemeResource SystemAccentColorLight1}" HorizontalAlignment="Right"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.Resources>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <!--<Setter Property="Margin" Value="10"/>-->
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplateSelector>
                                <data:ChatDataTemplateSelector
                            ChatDataTemplate="{StaticResource ChatDataTemplate}"
                            SelfChatDataTemplate="{StaticResource SelfChatDataTemplate}"/>
                            </ListView.ItemTemplateSelector>
                        </ListView>
                    </ScrollViewer>

                    <TextBox x:Uid="ChatBox" x:Name="chatbox" Grid.Row="1" PlaceholderText="User Requirements" AcceptsReturn="True" TextWrapping="Wrap"/>
                    <ComboBox x:Name="chattype" Grid.Row="2" SelectionChanged="ChatType_SelectionChanged" HorizontalAlignment="Stretch" Margin="0,0,100,0"/>
                    <Button x:Uid="ChatSubmit" Grid.Row="2" Content="Sumbit" Click="SubmitChat" Width="100" HorizontalAlignment="Right" Background="LightSkyBlue"/>
                </Grid>
            </toolkit:BladeItem>
        </toolkit:BladeView>

        <toolkit:InAppNotification x:Name="InAppNotification" 
                                    ShowDismissButton="True"
                                    AnimationDuration="00:00:00.1000000"
                                    VerticalOffset="100"
                                    HorizontalOffset="0"/>

    </Grid>

</Page>
