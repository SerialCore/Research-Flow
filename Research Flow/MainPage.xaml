<Page
    x:Class="Research_Flow.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Research_Flow"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="FullPage" Background="{StaticResource ApplicationPageBackgroundThemeBrush}">

        <NavigationView x:Name="NavView"
                        Background="{ThemeResource SystemAccentColorLight1}"
                        PaneDisplayMode="LeftCompact"
                        Loaded="NavView_Loaded"
                        SelectionChanged="NavView_SelectionChanged"
                        IsBackEnabled="{x:Bind ContentFrame.CanGoBack,Mode=OneWay}"
                        BackRequested="NavView_BackRequested">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Overview" Tag="Overview">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEB68;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItemSeparator/>
                <NavigationViewItem Content="Tag + Topic" Tag="TagTopic">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEB50;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Paper Box" Tag="PaperBox">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE7BE;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItemSeparator/>
                <NavigationViewItem Content="RSS Service" Tag="RSS">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE753;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Search Engine" Tag="Search">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEDE4;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Crawler Service" Tag="Crawler">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE24A;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItemSeparator/>
                <NavigationViewItem Content="Drawable Note" Tag="Note">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEA86;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.AutoSuggestBox>
                <AutoSuggestBox x:Name="ASB" QueryIcon="Find"/>
            </NavigationView.AutoSuggestBox>

            <NavigationView.PaneHeader>
                <TextBlock Text="  Research Flow" VerticalAlignment="Center"/>
            </NavigationView.PaneHeader>

            <NavigationView.PaneFooter>
                <StackPanel>
                    <ListViewItem Tapped="Flyout_Tapped">
                        <StackPanel Orientation="Horizontal">
                            <PersonPicture x:Name="accountPhoto" Height="20" Width="20"/>
                            <TextBlock x:Name="accountName" Margin="16,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <FlyoutBase.AttachedFlyout>
                            <Flyout>
                                <Grid Width="270">
                                    <Image Source="/Images/OneDrive_logo.png" VerticalAlignment="Top"/>
                                    <Grid Margin="0,90,0,0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                            <RowDefinition />
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="1" x:Name="accountEmail" FontSize="16" HorizontalAlignment="Center"/>
                                        <Button Grid.Row="2" x:Name="accountLogout" Content="Log out" Click="AccountLogout_Click" 
                                            Width="250" Margin="10" HorizontalAlignment="Right" Background="Red"/>
                                    </Grid>
                                </Grid>
                            </Flyout>
                        </FlyoutBase.AttachedFlyout>
                    </ListViewItem>
                    <StackPanel Orientation="Horizontal">
                        <AppBarButton Icon="AttachCamera" Tapped="Flyout_Tapped" ToolTipService.ToolTip="ScreenShot" Width="42" Height="42">
                            <FlyoutBase.AttachedFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="Export" Click="ScreenShot_Export"/>
                                    <MenuFlyoutItem Text="Share" Click="ScreenShot_Share"/>
                                    <MenuFlyoutItem Text="Upload" Click="ScreenShot_Upload"/>
                                </MenuFlyout>
                            </FlyoutBase.AttachedFlyout>
                        </AppBarButton>
                        <AppBarButton Icon="FullScreen" Click="FullScreen_Click" ToolTipService.ToolTip="FullScreen" Width="42" Height="42"/>
                        <AppBarButton Icon="Placeholder" Tapped="Flyout_Tapped" ToolTipService.ToolTip="Background" Width="42" Height="42">
                            <FlyoutBase.AttachedFlyout>
                                <Flyout Closed="ColorPicker_Closed">
                                    <ColorPicker x:Name="colorPicker" ColorChanged="ColorPicker_ColorChanged"/>
                                </Flyout>
                            </FlyoutBase.AttachedFlyout>
                        </AppBarButton>
                    </StackPanel>
                </StackPanel>
            </NavigationView.PaneFooter>

            <Frame x:Name="ContentFrame" Navigated="NavView_Navigated">
                <Frame.ContentTransitions>
                    <TransitionCollection>
                        <NavigationThemeTransition/>
                    </TransitionCollection>
                </Frame.ContentTransitions>
            </Frame>

        </NavigationView>

        <Grid CornerRadius="10" Height="32" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="48,0" Background="{ThemeResource SystemControlAccentAcrylicElementAccentMediumHighBrush}" Visibility="Collapsed">
            <TextBlock x:Name="appMessage" Text="Research Flow" FontSize="18" VerticalAlignment="Center" Margin="5,0,5,0"/>
        </Grid>

        <toolkit:InAppNotification x:Name="InAppNotification" 
                                    ShowDismissButton="True"
                                    AnimationDuration="00:00:00.1000000"
                                    VerticalOffset="100"
                                    HorizontalOffset="0"/>

    </Grid>

</Page>
