<Page
    x:Class="Research_Flow.PaperBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Research_Flow"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Pivot Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="0,0,0,40">
                    <PivotItem x:Uid="PaperDownloadPane" Header="Downloaded">
                        <ListView x:Name="pdftree" IsItemClickEnabled="True" ItemClick="Pdftree_ItemClick">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding }"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </PivotItem>
                    <PivotItem x:Uid="PaperDatabasePane" Header="Database">
                        <ListView x:Name="paperlist" IsItemClickEnabled="True" ItemClick="PaperList_ItemClick">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Title}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </PivotItem>
                </Pivot>
                <CommandBar VerticalAlignment="Bottom" Background="Transparent" DefaultLabelPosition="Right" OverflowButtonVisibility="Collapsed">
                    <CommandBar.Content>
                        <TextBlock x:Uid="PaperHeader" Text="Papers" Style="{ThemeResource BodyTextBlockStyle}" Margin="12,10,0,0"/>
                    </CommandBar.Content>
                    <AppBarButton x:Uid="PaperExport" Icon="Share" Label="Export">
                        <AppBarButton.Flyout>
                            <MenuFlyout>
                                <MenuFlyoutItem x:Uid="PaperShare" Text="Share" Click="Pdf_Share"/>
                                <MenuFlyoutItem x:Uid="PaperSaveAs" Text="SaveAs" Click="Pdf_Export"/>
                            </MenuFlyout>
                        </AppBarButton.Flyout>
                    </AppBarButton>
                    <AppBarButton x:Uid="PaperImport" Icon="Import" Label="Import" Click="Pdf_Import"/>
                    <AppBarButton x:Uid="PaperPreview" Icon="Preview" Label="Preview" Click="Pdf_Preview"/>
                </CommandBar>
            </Grid>

            <Grid Grid.Column="1" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                <StackPanel Margin="10,10,10,40">
                    <TextBox x:Uid="PaperTitle" x:Name="papertitle" Header="Title" IsSpellCheckEnabled="False"/>
                    <TextBox x:Name="paperid" Header="ID, doi, arXiv" IsSpellCheckEnabled="False"/>
                    <AutoSuggestBox x:Uid="PaperWebSite" x:Name="paperlink" Header="Website" QueryIcon="Find" QuerySubmitted="PaperLink_QuerySubmitted"/>
                    <TextBox x:Uid="PaperPublished" x:Name="paperdate" Header="Published" IsSpellCheckEnabled="False"/>
                    <TextBox x:Uid="PaperAuthors" x:Name="paperauthor" Header="Authors" PlaceholderText="Author1, Author2, Author3" IsSpellCheckEnabled="False"/>
                    <TextBox x:Uid="PaperNote" x:Name="papernote" Header="Paper Note" PlaceholderText="reason to refer" IsSpellCheckEnabled="False" AcceptsReturn="True" TextWrapping="Wrap" Height="200"/>
                    <TextBox x:Uid="PaperTags" x:Name="papertags" Header="Paper Tags" PlaceholderText="#Tag1##Tag2#" IsSpellCheckEnabled="False" AcceptsReturn="True" TextWrapping="Wrap" Height="100"/>
                    <TextBox x:Uid="PaperFile" x:Name="pdfname" Header="Paper File" IsSpellCheckEnabled="False"/>
                </StackPanel>
                <CommandBar VerticalAlignment="Bottom" Background="Transparent" DefaultLabelPosition="Right" OverflowButtonVisibility="Collapsed">
                    <AppBarButton x:Uid="SavePaper" Icon="Save" Label="Save">
                        <AppBarButton.Flyout>
                            <MenuFlyout>
                                <MenuFlyoutItem x:Uid="RenamePdf" Text="Rename Pdf" Click="SavePdfFile"/>
                                <MenuFlyoutItem x:Uid="SaveToDatabase" Text="To Database" Click="SavePaper"/>
                            </MenuFlyout>
                        </AppBarButton.Flyout>
                    </AppBarButton>
                    <AppBarButton x:Uid="DeletePaper" Icon="Delete" Label="Delete" Click="DeletePaper"/>
                </CommandBar>
            </Grid>

        </Grid>
    </Grid>

</Page>
