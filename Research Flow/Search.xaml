<Page
    x:Class="Research_Flow.Search"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Research_Flow"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid >
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="10">
                <Grid Height="48" VerticalAlignment="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Text="Bing" VerticalAlignment="Center"/>
                    <ComboBox Grid.Column="1" x:Name="searchlist" VerticalAlignment="Center"/>
                    <AutoSuggestBox Grid.Column="2" x:Name="queryQuest" Text="https://cn.bing.com/search?q=QUEST"
                                QueryIcon="Find" QuerySubmitted="AutoSuggestBox_QuerySubmitted" VerticalAlignment="Center"/>
                </Grid>

                <WebView x:Name="webview" Margin="0,48,0,0"/>

            </Grid>

            <CommandBar Grid.Row="1" Background="Transparent">
                
            </CommandBar>
            
        </Grid>
        

        <toolkit:InAppNotification x:Name="InAppNotification" 
                                    ShowDismissButton="True"
                                    AnimationDuration="00:00:00.1000000"
                                    VerticalOffset="100"
                                    HorizontalOffset="0"/>

    </Grid>

</Page>
