<Page
    x:Class="Research_Flow.Search"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Research_Flow"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Pivot Margin="20,0,20,10">
            <PivotItem Header="Flow">
                <StackPanel Height="500" Width="500">
                    <StackPanel Orientation="Horizontal">
                        <TextBox Grid.Row="1" Grid.Column="0" Text="Bing"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="https://cn.bing.com/search?q=QUEST"/>
                    </StackPanel>
                </StackPanel>
            </PivotItem>
            <PivotItem Header="RSS">
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0" Background="{ThemeResource AppBarBackgroundThemeBrush}">
                        <ListView x:Name="feedSource_list" IsItemClickEnabled="True" ItemClick="RSS_SourceClick"
                                         ReorderMode="Enabled" Margin="0,0,0,72">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="24"/>
                                            <ColumnDefinition Width="204"/>
                                            <ColumnDefinition Width="72"/>
                                        </Grid.ColumnDefinitions>
                                        <FontIcon Grid.Column="0" FontFamily="Segoe MDL2 Assets" Glyph="&#xE753;" />
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                                        <AppBarButton Grid.Column="2" Icon="More" Click="Modify_Source" Tag="{Binding ID}" Width="48" LabelPosition="Collapsed"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <Button Content="Add Source" Click="Add_Source"
                                    Height="32" Width="250" VerticalAlignment="Bottom" Margin="20" Background="LightBlue"/>
                        <Grid x:Name="source_panel" Visibility="Collapsed" Background="{ThemeResource AppBarBackgroundThemeBrush}" Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" x:Name="rssName" Header="Title" IsSpellCheckEnabled="False" Margin="0,4"/>
                            <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" x:Name="rssUrl" Header="Url" IsSpellCheckEnabled="False" Margin="0,4"/>
                            <Slider Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" x:Name="feedCount" Header="Max count of items" Minimum="20" Maximum="100" TickFrequency="10" TickPlacement="Outside" Margin="0,4"/>
                            <Slider Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" x:Name="rssDays" Header="Days for update" Minimum="1" Maximum="10" TickFrequency="1" TickPlacement="Outside" Margin="0,4"/>
                            <CheckBox Grid.Row="4" Grid.Column="0" x:Name="isJournal" Content="Is journal" Margin="0,4"/>
                            <RatingControl Grid.Row="4" Grid.Column="1" x:Name="rssStar"/>
                            <Button Grid.Row="6" Grid.Column="0" Click="Confirm_FeedSetting" Content="Confirm" Margin="10" HorizontalAlignment="Stretch" Background="LightSkyBlue"/>
                            <Button Grid.Row="6" Grid.Column="1" Click="Leave_FeedSetting" Content="Cancel" Margin="10" HorizontalAlignment="Stretch" Background="LightGray"/>
                            <Button Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" x:Name="rssDelete" Click="Delete_RSSSetting" Content="Delete" Margin="10" HorizontalAlignment="Stretch" Background="Red" Visibility="Collapsed"/>
                        </Grid>
                    </Grid>
                    <Grid Grid.Column="1">
                        <ListView x:Name="feedItem_list" IsItemClickEnabled="True" ItemClick="Feed_ItemClick">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel >
                                        <TextBlock Text="{Binding Title}" FontSize="20" Style="{ThemeResource BodyTextBlockStyle}"/>
                                        <TextBlock Text="{Binding Summary}" FontSize="16" MaxLines="2"/>
                                        <TextBlock Text="{Binding Published}"/>
                                        <HyperlinkButton Content="{Binding Link}" NavigateUri="{Binding Link}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <ProgressRing x:Name="waiting_feed" Height="100" Width="100"/>
                    </Grid>
                    <SplitView Grid.Column="0" Grid.ColumnSpan="2" x:Name="feedItem_detail" OpenPaneLength="400" DisplayMode="Overlay" PanePlacement="Right">
                        <SplitView.Pane>
                            <Grid>
                                <Button Height="32" Click="Close_FeedDetail" VerticalAlignment="Top" HorizontalAlignment="Stretch" Background="LightBlue">
                                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE0AB;" />
                                </Button>
                                <ScrollViewer Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="10,0" Margin="0,32">
                                    <StackPanel>
                                        <TextBlock x:Name="feedTitle" FontSize="20" Style="{ThemeResource TitleTextBlockStyle}"/>
                                        <Button x:Name="feedBrowse" Click="Browse_Feed" Content="Browse this feed" Background="GreenYellow"/>
                                        <TextBlock x:Name="feedPublished" FontSize="16" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                                        <TextBlock x:Name="feedSummary" FontSize="16" Style="{ThemeResource BodyTextBlockStyle}"/>
                                        <TextBlock x:Name="feedNodes" IsTextSelectionEnabled="True" FontSize="16" Style="{ThemeResource BodyTextBlockStyle}"/>
                                    </StackPanel>
                                </ScrollViewer>
                                <Button Height="32" Content="Learn this Feed" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Background="LightSkyBlue"/>
                            </Grid>
                        </SplitView.Pane>
                    </SplitView>
                </Grid>
            </PivotItem>
            <PivotItem Header="Crawler">

            </PivotItem>
        </Pivot>

        <toolkit:InAppNotification x:Name="InAppNotification" 
                                    ShowDismissButton="True"
                                    AnimationDuration="00:00:00.1000000"
                                    VerticalOffset="100"
                                    HorizontalOffset="0"/>

    </Grid>

</Page>
