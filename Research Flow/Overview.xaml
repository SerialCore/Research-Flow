<Page
    x:Class="Research_Flow.Overview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Research_Flow"
    xmlns:object="using:LogicService.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="32"/>
                </Grid.RowDefinitions>
                <Image Source="ms-appx:///Images/ResearchFlow_logo.jpg"/>

                <ListView x:Name="messagelist" Grid.Row="0" SelectionMode="None">
                    <ListView.Resources>
                        <DataTemplate x:Key="MessageDataTemplate">
                            <Grid HorizontalAlignment="Left" Margin="0,5,0,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0" CornerRadius="4" Background="{ThemeResource SystemAccentColorDark1}" Padding="10">
                                    <TextBlock Text="{Binding Comment}" Foreground="White" IsTextSelectionEnabled="True" TextWrapping="Wrap" MaxWidth="300"/>
                                </Grid>
                                <TextBlock Grid.Row="1" Text="{Binding Published}" Foreground="{ThemeResource SystemAccentColorDark1}"/>
                            </Grid>
                        </DataTemplate>

                        <DataTemplate x:Key="SelfMessageDataTemplate">
                            <Grid HorizontalAlignment="Right" Margin="0,5,0,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0" CornerRadius="4" Background="{ThemeResource SystemAccentColorLight1}" Padding="10">
                                    <TextBlock Text="{Binding Comment}" Foreground="White" IsTextSelectionEnabled="True" TextWrapping="Wrap" MaxWidth="300"/>
                                </Grid>
                                <TextBlock Grid.Row="1" Text="{Binding Published}" Foreground="{ThemeResource SystemAccentColorLight1}" HorizontalAlignment="Right"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.Resources>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <!--<Setter Property="Margin" Value="10"/>-->
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplateSelector>
                        <object:MessageItemDataTemplateSelector 
                            MessageDataTemplate="{StaticResource MessageDataTemplate}" 
                            SelfMessageDataTemplate="{StaticResource SelfMessageDataTemplate}" />
                    </ListView.ItemTemplateSelector>
                </ListView>

                <TextBox Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap"/>
                <ComboBox Grid.Row="2" HorizontalAlignment="Stretch" Margin="0,0,100,0"/>
                <Button Grid.Row="2" Content="Sumbit" Height="32" Width="100" HorizontalAlignment="Right" Background="LightSkyBlue"/>
            </Grid>

        </Grid>

    </Grid>

</Page>
