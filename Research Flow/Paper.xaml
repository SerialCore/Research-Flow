<Page
    x:Class="Research_Flow.Paper"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Research_Flow"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <TextBlock Text="Panel" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>

            <SplitView Grid.Column="1" PanePlacement="Right" OpenPaneLength="400" IsPaneOpen="True">
                <SplitView.Content>
                    <ListView>

                    </ListView>
                </SplitView.Content>
                <SplitView.Pane>
                    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                        <TextBlock Text="Detials" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </SplitView.Pane>
            </SplitView>
            
        </Grid>
        
    </Grid>
    
</Page>
