<Page
    x:Class="Research_Flow.Crawler"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Research_Flow"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="1">
                <TextBlock x:Name="msg" Text="Flow | Pool" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="12,0,0,0"/>
                <SplitView PanePlacement="Right" OpenPaneLength="400" DisplayMode="Overlay">
                    <SplitView.Pane>
                        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

                        </Grid>
                    </SplitView.Pane>
                    <SplitView.Content>
                        <Grid />
                    </SplitView.Content>
                </SplitView>
            </Grid>
        </Grid>

    </Grid>
    
</Page>
