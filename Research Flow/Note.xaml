<Page
    x:Class="Research_Flow.Note"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Research_Flow"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <toolkit:InfiniteCanvas x:Name="canvas" Grid.Row="0"/>

            <CommandBar Grid.Row="1" Background="Transparent" DefaultLabelPosition="Right" OverflowButtonVisibility="Collapsed">
                <AppBarButton Icon="Share" Label="Share" Click="Share_Note"/>
                <AppBarButton Icon="Pin" Label="ToImage" Click="Share_Note"/>
                <AppBarSeparator />
                <AppBarButton Icon="Save" Label="Save" Click="Save_Note"/>
                <AppBarButton Icon="ZoomOut" Label="Export" Click="Export_Note"/>
                <AppBarButton Icon="Import" Label="Import" Click="Import_Note"/>
                <AppBarButton Icon="Document" Label="Document" Click="Open_Document"/>
            </CommandBar>
        </Grid>

        <toolkit:InAppNotification x:Name="InAppNotification" 
                                    ShowDismissButton="True"
                                    AnimationDuration="00:00:00.1000000"
                                    VerticalOffset="100"
                                    HorizontalOffset="0"/>

    </Grid>

</Page>
